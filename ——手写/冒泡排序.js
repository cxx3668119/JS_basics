let ary = [12, 8, 24, 16, 1];

function bubble(ary) {
  //外层循环 控制比较轮数
  for (let i = 0; i < ary.length - 1; i++) {
    //内层控制每一轮比较次数
    for (let j = 0; j < ary.length - 1 - i; j++) {
      if (ary[j] > ary[j + 1]) {
        
      }
    }
  }
}